[package]
name = "shared"
version = "0.1.0"
edition = "2021"

[dependencies]
gin-tonic = { path = "../../gin", version = "0.1.0" }
tonic = "0.11.0"

[build-dependencies]
gin-tonic = { path = "../../gin", version = "0.1.0" }

[lints]
workspace = true
